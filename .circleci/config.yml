version: 2
jobs:
    build:
        docker:
          - image: cimg/php:8.0
          - image: cimg/mysql:8.0
        steps:
            - checkout
            - run:
                name: Echo Test
                command: echo "CircleCI Test"
    deploy:
        machine:
            - ubuntu-2004:202010-01
        steps:
            - checkout
            - add_ssh_keys:
                fingerprints:
                  - b0:8d:e3:84:b9:66:06:20:00:7a:b0:34:28:84:65:df

            - run: ssh ${USER_NAME}@${HOST_NAME} 'cd myPortofolio && git pull'

workflows:
    version: 2
    build_and_deploy:
        jobs:
            - build
            - deploy:
                requires:
                    - build
                filters:
                    branches:
                        only: main
