version: 2
jobs:
    build:
        docker:
          - image: cimg/php:8.0
          - image: cimg/mysql:8.0
        steps:
            - checkout
            - run:
                name: Echo Test
                command: echo "CircleCI Test"
    deploy:
        machine:
            - ubuntu-2004:202010-01
        steps:
            - checkout
            - add_ssh_keys:
                fingerprints:
                  - b2:ad:da:99:70:31:a0:66:7a:c4:7f:99:0d:a0:3c:f4

            - run: ssh ${USER_NAME}@${HOST_NAME} 'cd myPortofolio && git pull'

workflows:
    version: 2
    build_and_deploy:
        jobs:
            - build
            - deploy:
                requires:
                    - build
                filters:
                    branches:
                        only: main
